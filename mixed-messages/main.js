// Object that store multiple sentence parts in arrays
const sentencePart = {
  firstPart: [],
  secondPart: [],
  thirdPart: [],
};

/**
 * Return a random element in the parameter array
 * @param {} array 
 */
const randomElement = (array) => array[Math.floor(Math.random() * array.length)];


